<div class="hero min-h-screen relative bg-[center_center] md:bg-[center_60_center]  " style={{backgroundImage: `url(${heroImage})`}}>
    <div class="hero absolute bottom-0 pt-10 pb-5 bg-gradient-to-t from-neutral to-transparent text-white grid">
        <div class="prose overflow-hidden text-center mt-5 mx-10">
            <h1 class="drop-in text-base-100 drop-shadow">{title}</h1>
            <p class="drop-in-2 text-base-100 drop-shadow">{synopsis}</p>  
            <div class="flex justify-center items-center py-3">
                <ArrowDownIcon size="20" fill="#eff0f6" class="animate-bounce" alt="arrow down" /> 
            </div>
        </div>
    </div>
</div>