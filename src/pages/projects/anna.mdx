---
layout: "../../layouts/BaseLayout.astro"
---
import { Picture } from "astro-imagetools/components";
import ProjectHero from '../../components/ProjectHero.astro';
export const meta = {
    title: 'Anna',
    synopsis: 'One night, Anna is confronted with an attack by an unknown man, which both brings her closer to her fears as well as her confidence to fight them.',
    heroImage: '/img/anna/stills/20181119_Anna_1.74.1.webp',
};

<ProjectHero title={meta.title} synopsis={meta.synopsis} heroImage={meta.heroImage} />

<section class="hero">    
    <div class="hero-content">
        <Picture
            src='/img/anna/stills/20181119_Anna_1.76.1.webp'
            sizes={(breakpoints) => {
                const maxWidth = breakpoints[breakpoints.length - 1];
                return `(min-width: ${maxWidth}px) ${maxWidth}px, 100vw`;
            }}
            breakpoints={[600, 1000, 1200]}
            loading="lazy"   
            format={["avif", "webp"]}
            quality="90"
            alt="Anna cinematography still"        
        />
    </div>
</section>

<section class="hero" >
    <div id="credits" class="hero-content grid grid-cols-2 gap-2 text-base-200">            
        <p class="text-right font-light">Director</p>
        <p class="text-left font-bold">Robin Hendrix</p>
        <p class="text-right font-light">Cast</p>
        <p class="text-left font-bold">Shauni Goetz</p>
        <p class="text-right font-light">Cinematographer</p>
        <p class="text-left font-bold">Victor Maes</p>
        <p class="text-right font-light">Assistant Director</p>
        <p class="text-left font-bold">Toon Minnen</p>
        <p class="text-right font-light">Sound</p>
        <p class="text-left font-bold">Oscar Schutzr</p>
    </div>  
</section>

<section class="hero">    
    <div class="hero-content flex-col">
        <Picture
            src='/img/anna/stills/20181119_Anna_1.77.1.webp'
            sizes={(breakpoints) => {
                const maxWidth = breakpoints[breakpoints.length - 1];
                return `(min-width: ${maxWidth}px) ${maxWidth}px, 100vw`;
            }}
            breakpoints={[600, 1000, 1200]}
            loading="lazy"   
            format={["avif", "webp"]}
            quality="90"
            alt="Anna cinematography still"        
        />
        <Picture
            src='/img/anna/stills/20181119_Anna_1.69.1.webp'
            sizes={(breakpoints) => {
                const maxWidth = breakpoints[breakpoints.length - 1];
                return `(min-width: ${maxWidth}px) ${maxWidth}px, 100vw`;
            }}
            breakpoints={[600, 1000, 1200]}
            loading="lazy"   
            format={["avif", "webp"]}
            quality="90"
            alt="Anna cinematography still"        
        />
        <Picture
            src='/img/anna/stills/20181119_Anna_1.70.1.webp'
            sizes={(breakpoints) => {
                const maxWidth = breakpoints[breakpoints.length - 1];
                return `(min-width: ${maxWidth}px) ${maxWidth}px, 100vw`;
            }}
            breakpoints={[600, 1000, 1200]}
            loading="lazy"   
            format={["avif", "webp"]}
            quality="90"
            alt="Anna cinematography still"        
        />
        <Picture
            src='/img/anna/stills/20181119_Anna_1.71.2.webp'
            sizes={(breakpoints) => {
                const maxWidth = breakpoints[breakpoints.length - 1];
                return `(min-width: ${maxWidth}px) ${maxWidth}px, 100vw`;
            }}
            breakpoints={[600, 1000, 1200]}
            loading="lazy"   
            format={["avif", "webp"]}
            quality="90"
            alt="Anna cinematography still"        
        />
        <Picture
            src='/img/anna/stills/20181119_Anna_1.78.1.webp'
            sizes={(breakpoints) => {
                const maxWidth = breakpoints[breakpoints.length - 1];
                return `(min-width: ${maxWidth}px) ${maxWidth}px, 100vw`;
            }}
            breakpoints={[600, 1000, 1200]}
            loading="lazy"   
            format={["avif", "webp"]}
            quality="90"
            alt="Anna cinematography still"        
        />
        <Picture
            src='/img/anna/stills/20181119_Anna_1.74.1.webp'
            sizes={(breakpoints) => {
                const maxWidth = breakpoints[breakpoints.length - 1];
                return `(min-width: ${maxWidth}px) ${maxWidth}px, 100vw`;
            }}
            breakpoints={[600, 1000, 1200]}
            loading="lazy"   
            format={["avif", "webp"]}
            quality="90"
            alt="Anna cinematography still"        
        />
    </div>
</section>