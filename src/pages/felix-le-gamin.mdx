---
layout: "../layouts/BaseLayout.astro"
---
import { Picture } from "astro-imagetools/components";
import ProjectHero from '../components/ProjectHero.astro';
export const title = 'Felix, le gamin qui traverse'
export const synopsis = 'A fragment of the lives of Felix and Boris - two young men who meet again and unexpectedly share a moment of grief.'
export const heroImage = '/img/felix/stills/20190221_LeGaminQuiTraverse_1.57.1.webp'

<section>
    <ProjectHero title={title} synopsis={synopsis} heroImage={heroImage} />
</section>

<section class="hero">    
    <div class="hero-content flex-col">
        <Picture
            src='/img/felix/stills/20190221_LeGaminQuiTraverse_1.91.2.webp'
            sizes={(breakpoints) => {
                const maxWidth = breakpoints[breakpoints.length - 1];
                return `(min-width: ${maxWidth}px) ${maxWidth}px, 100vw`;
            }}
            breakpoints={[600, 1000, 1200]}
            loading="lazy"   
            format={["avif", "webp"]}
            quality="90"
            alt="Felix Le Gamin Qui Traverse cinematography still"        
        />
    </div>
</section>

<section class="hero" >
    <div id="credits" class="hero-content grid grid-cols-2 gap-2 text-base-200">        
        <p class="text-right font-light">Director</p>
        <p class="text-left font-bold">Max Meyer</p>
        <p class="text-right font-light">Cast</p>
        <p class="text-left font-bold">Felix Meyer</p>
        <p class="text-right font-light"></p>
        <p class="text-left font-bold">Yoan Zimmer</p>
        <p class="text-right font-light">Cinematographer</p>
        <p class="text-left font-bold">Victor Maes</p>
        <p class="text-right font-light">1st Assistant Camera</p>
        <p class="text-left font-bold">Jan-Willem Rouwhorst</p>
        <p class="text-right font-light">Assistant Director</p>
        <p class="text-left font-bold">Kate Voet</p>
        <p class="text-right font-light">Sound</p>
        <p class="text-left font-bold">Gillis van der Wee</p>
    </div>  
</section>

<section class="hero">    
    <div class="hero-content flex-col">
        <Picture
            src='/img/felix/stills/20190221_LeGaminQuiTraverse_1.15.1.webp'
            sizes={(breakpoints) => {
                const maxWidth = breakpoints[breakpoints.length - 1];
                return `(min-width: ${maxWidth}px) ${maxWidth}px, 100vw`;
            }}
            breakpoints={[600, 1000, 1200]}
            loading="lazy"   
            format={["avif", "webp"]}
            quality="90"
            alt="Felix Le Gamin Qui Traverse cinematography still"        
        />
        <Picture
            src='/img/felix/stills/20190221_LeGaminQuiTraverse_1.47.1.webp'
            sizes={(breakpoints) => {
                const maxWidth = breakpoints[breakpoints.length - 1];
                return `(min-width: ${maxWidth}px) ${maxWidth}px, 100vw`;
            }}
            breakpoints={[600, 1000, 1200]}
            loading="lazy"   
            format={["avif", "webp"]}
            quality="90"
            alt="Felix Le Gamin Qui Traverse cinematography still"        
        />
        <Picture
            src='/img/felix/stills/20190221_LeGaminQuiTraverse_1.46.3.webp'
            sizes={(breakpoints) => {
                const maxWidth = breakpoints[breakpoints.length - 1];
                return `(min-width: ${maxWidth}px) ${maxWidth}px, 100vw`;
            }}
            breakpoints={[600, 1000, 1200]}
            loading="lazy"   
            format={["avif", "webp"]}
            quality="90"
            alt="Felix Le Gamin Qui Traverse cinematography still"        
        />
        <Picture
            src='/img/felix/stills/20190221_LeGaminQuiTraverse_1.50.4.webp'
            sizes={(breakpoints) => {
                const maxWidth = breakpoints[breakpoints.length - 1];
                return `(min-width: ${maxWidth}px) ${maxWidth}px, 100vw`;
            }}
            breakpoints={[600, 1000, 1200]}
            loading="lazy"   
            format={["avif", "webp"]}
            quality="90"
            alt="Felix Le Gamin Qui Traverse cinematography still"        
        />
        <Picture
            src='/img/felix/stills/20190221_LeGaminQuiTraverse_1.52.1.webp'
            sizes={(breakpoints) => {
                const maxWidth = breakpoints[breakpoints.length - 1];
                return `(min-width: ${maxWidth}px) ${maxWidth}px, 100vw`;
            }}
            breakpoints={[600, 1000, 1200]}
            loading="lazy"   
            format={["avif", "webp"]}
            quality="90"
            alt="Felix Le Gamin Qui Traverse cinematography still"        
        />
        <Picture
            src='/img/felix/stills/20190221_LeGaminQuiTraverse_1.57.1.webp'
            sizes={(breakpoints) => {
                const maxWidth = breakpoints[breakpoints.length - 1];
                return `(min-width: ${maxWidth}px) ${maxWidth}px, 100vw`;
            }}
            breakpoints={[600, 1000, 1200]}
            loading="lazy"   
            format={["avif", "webp"]}
            quality="90"
            alt="Felix Le Gamin Qui Traverse cinematography still"        
        />
        <Picture
            src='/img/felix/stills/20190221_LeGaminQuiTraverse_1.5.4.webp'
            sizes={(breakpoints) => {
                const maxWidth = breakpoints[breakpoints.length - 1];
                return `(min-width: ${maxWidth}px) ${maxWidth}px, 100vw`;
            }}
            breakpoints={[600, 1000, 1200]}
            loading="lazy"   
            format={["avif", "webp"]}
            quality="90"
            alt="Felix Le Gamin Qui Traverse cinematography still"        
        />
    </div>
</section>